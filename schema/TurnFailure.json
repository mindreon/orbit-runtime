{
  "$defs": {
    "TurnErrorCode": {
      "description": "Why a failed turn failed. Clients map these to text and never parse ``error``.",
      "enum": [
        "timeout",
        "auth",
        "rate_limited",
        "provider_error",
        "config",
        "state_unreadable"
      ],
      "title": "TurnErrorCode",
      "type": "string"
    }
  },
  "description": "Payload of a ``turn.failed`` event: ``{turnId, agentId, errorCode, retryable, message}``.\n\n``agent_id`` is the contract agent id (``main`` for the room agent), not\nthe session id. ``message`` is the fixed text for ``error_code``, never\nbuilt from provider or exception text.",
  "properties": {
    "agentId": {
      "title": "Agentid",
      "type": "string"
    },
    "errorCode": {
      "$ref": "#/$defs/TurnErrorCode"
    },
    "message": {
      "title": "Message",
      "type": "string"
    },
    "retryable": {
      "title": "Retryable",
      "type": "boolean"
    },
    "turnId": {
      "title": "Turnid",
      "type": "string"
    }
  },
  "required": [
    "turnId",
    "agentId",
    "errorCode",
    "retryable",
    "message"
  ],
  "title": "TurnFailure",
  "type": "object"
}
